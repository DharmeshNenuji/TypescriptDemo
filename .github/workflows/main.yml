name: react-native-android-build-apk
on:
  push:
    branches:
      - master
jobs:
  install-and-test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install npm dependencies
        run: |
          npm install
      - name: Run Lint
        run: |
          npm run linter
      - name: Cache Gradle
        uses: actions/cache@v2
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle') }}
      - name: Set up JDK
        uses: actions/setup-java@v2
        with:
          java-version: '11'
          distribution: 'adopt'
      - name: Grant execute permission for Gradle
        run: cd android && chmod +x ./gradlew

      - name: Build with Gradles
        run: cd android && ./gradlew assembleDebug

      - name: Upload APK TestApp.io
        uses: testappio/github-action@v5
        with:
          api_token: ${{secrets.DEMO_APP_IO_API_TOKEN}}
          app_id: ${{secrets.DEMO_APP_IO_APP_ID}}
          file: Users/mac-3/Documents/Projects/Example/CICDDemo/TypescriptDemo/android/app/build/outputs/apk/debug/app-debug.apk
          release_notes: 'test release'
          git_release_notes: true
          include_git_commit_id: false
          notify: true
